'use client';

/**
 * @deprecated The reports page moved to `src/components/reports/` (MUI, presets,
 * printable PDF sheet). This file only re-exports the page client so old
 * imports keep working; delete it once nothing references it.
 */
export { ReportsPageClient as ReportsClient } from '@/components/reports/reports-page-client';
