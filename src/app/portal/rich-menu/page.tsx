import { RichMenuBuilder } from '@/components/rich-menu/rich-menu-builder';

export const metadata = { title: 'สร้าง Rich Menu' };

/** Rich Menu master template builder. */
export default function RichMenuPage() {
  return <RichMenuBuilder />;
}
